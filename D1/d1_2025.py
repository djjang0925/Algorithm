# from functools import reduce
# num = int(input())
# print(reduce(lambda x, y: x + y, range(num + 1)))

# n = int(input())
# print((1 + n) * n // 2)

n = int(input())
list = list(range(1, n + 1))
print(sum(list))